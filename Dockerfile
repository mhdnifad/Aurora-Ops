# Root Dockerfile for Render (multi-service via docker-compose)
# This file is a placeholder to satisfy Render's Docker build detection.
# Render should use docker-compose.yml for multi-service deployment.
FROM scratch
